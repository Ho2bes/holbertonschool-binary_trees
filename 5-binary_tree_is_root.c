#include "binary_trees.h"

/**
 * binary_tree_is_root - Checks if a node is a root of a binary tree.
 * @node: A pointer to the node to check.
 *
 * Return: If the node is a root return 1 or 0 if it's not.
 */

{
	if (node == NULL)
		return (0);
	if else (node->parent == NULL)
		return (1);
	else
		return (0);

}
